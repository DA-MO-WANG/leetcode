//
// Created by rich heart on 03/09/2022.
//

#include <cstring>
#include <iostream>
#include <algorithm>

using namespace std;
int f[N],g[N],q[N];
int main() {
    cin >> n >> m;
    for (int i = 0; i < n; ++i) {
        int v, w, s;
        cin >> v >> w >> s;
        memcpy(g, f, sizeof(f));
        for (int j = 0; j < v; ++j) {
            int hh = 0, tt = -1;
            for (int k = j; k <= m; ++k) {
                if()
            }
        }
    }
    return 0;
}