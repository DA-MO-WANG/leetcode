//
// Created by rich heart on 31/07/2022.
//

#include <cstring>
#include <iostream>
#include <algorithm>

using namespace std;
struct ListNode {
    int val;
    ListNode *next;
    ListNode(int x) : val(x), next(NULL) {}
};
ListNode *entryNodeOfLoop(ListNode *head) {
    //需要两个指针，一个指针在前面，一个指针在后面
    //只有存在环的情况下，前面的指针才有可能走后面的后面
    int 
}
int main() {

    return 0;
}